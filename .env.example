# Cloudflare API token with Workers Scripts:Edit (and Zone -> Workers Routes
# if you attach a custom domain in wrangler.toml).
# Generate at dash.cloudflare.com -> My Profile -> API Tokens.
# Copy this file to .env — .env is gitignored and must stay that way.
CLOUDFLARE_API_TOKEN=
